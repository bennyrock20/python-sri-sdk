<?xml version="1.0" encoding="UTF-8"?>
<factura id="comprobante" version="1.1.0">
       <infoTributaria>
        <ambiente>{{ bill.environment.value }}</ambiente>
        <tipoEmision>{{ bill.emission_type.value }}</tipoEmision>
        <razonSocial>{{ bill.billing_name}}</razonSocial>
        <nombreComercial>{{ bill.company_name}}</nombreComercial>
        <ruc>{{ bill.company_ruc }}</ruc>
        <claveAcceso>{{ claveAcceso }}</claveAcceso>
        <codDoc>01</codDoc>
        <estab>{{  bill.establishment }}</estab>
        <ptoEmi>{{ bill.point_emission }}</ptoEmi>
        <secuencial>{{ bill.sequential }}</secuencial>
        <dirMatriz>{{ bill.main_address }}</dirMatriz>
    </infoTributaria>
    <infoFactura>
        <fechaEmision>{{ fechaEmision }}</fechaEmision>
        <dirEstablecimiento>{{ bill.company_address }}</dirEstablecimiento>
        <obligadoContabilidad>{{ bill.company_obligado_contabilidad }}</obligadoContabilidad>
        <tipoIdentificacionComprador>{{ bill.customer_identification_type.value }}</tipoIdentificacionComprador>
        <razonSocialComprador>{{bill.customer_billing_name}}</razonSocialComprador>
        <identificacionComprador>{{ bill.customer_identification}}</identificacionComprador>
        <direccionComprador>{{  bill.customer_address }}</direccionComprador>
        <totalSinImpuestos>{{ bill.total_without_tax }}</totalSinImpuestos>
        <totalDescuento>{{ bill.total_discount }}</totalDescuento>
        <totalConImpuestos>{% for i in bill.taxes %}
            <totalImpuesto>
                <codigo>{{ i.code.value }}</codigo>
                <codigoPorcentaje>{{ i.tax_percentage_code.value }}</codigoPorcentaje>
                <descuentoAdicional>{{ i.additional_discount }}</descuentoAdicional>
                <baseImponible>{{ i.base }}</baseImponible>
                <valor>{{ i.value }}</valor>
            </totalImpuesto>{% endfor %}
        </totalConImpuestos>
        <propina>{{ bill.tips }}</propina>
        <importeTotal>{{ bill.grand_total }} </importeTotal>
        <moneda>DOLAR</moneda>
        <pagos>{% for i in bill.payments %}
            <pago>
                <formaPago>{{ i.payment_method.value }}</formaPago>
                <total>{{ i.total }}</total>
                <plazo>{{ i.terms }}</plazo>
                <unidadTiempo>{{ i.unit_time.value }}</unidadTiempo>
            </pago>{% endfor %}
        </pagos>
    </infoFactura>
    <detalles>{% for i in bill.lines_items %}
        <detalle>
            <codigoPrincipal>{{ i.code }}</codigoPrincipal>
            <codigoAuxiliar>{{ i.aux_code }}</codigoAuxiliar>
            <descripcion>{{ i.description }}</descripcion>
            <cantidad>{{ i.quantity }}</cantidad>
            <precioUnitario>{{ i.unit_price }}</precioUnitario>
            <descuento>{{ i.discount }}</descuento>
            <precioTotalSinImpuesto>{{ i.price_total_without_tax }}</precioTotalSinImpuesto>
            <impuestos>{% for j in i.taxes %}
                <impuesto>
                    <codigo>{{ j.code.value }}</codigo>
                    <codigoPorcentaje>{{ j.tax_percentage_code.value }}</codigoPorcentaje>
                    <tarifa>{{ j.tarifa }}</tarifa>
                    <baseImponible>{{ j.base }}</baseImponible>
                    <valor>{{ j.value }}</valor>
                </impuesto>{% endfor %}
            </impuestos>
        </detalle>{% endfor %}
    </detalles>
    <tipoNegociable>
        <correo>bill@rushdelivery.app</correo>
    </tipoNegociable>
</factura>